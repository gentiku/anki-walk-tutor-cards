# German Walk Tutor cards

A small public German-English test deck for checking whether ChatGPT can read external study material before a Live Voice conversation.

The machine-readable deck is in [`cards.txt`](./cards.txt).

Run `node export-from-anki.mjs` while Anki is open to refresh up to 60 currently due cards through the local AnkiConnect service.
